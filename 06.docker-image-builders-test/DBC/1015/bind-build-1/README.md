# Buildah in Docker DBC 10.15 container build test

This test shows how to build a DBC 10.15 minimal container using rootless buildah.

At the end of the test, the resulting image is not pushed anywhere.